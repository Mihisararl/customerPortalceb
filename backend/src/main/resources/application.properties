# Spring Boot Application Configuration

# Server Configuration
server.port=8080
server.servlet.context-path=/api

# Application Name
spring.application.name=ceb-customer-portal-backend

# Security Configuration
security.jwt.secret=your-super-secret-jwt-key-change-this-in-production-must-be-at-least-256-bits-long
security.jwt.expiration=3600000

# Logging Configuration
logging.level.root=INFO
logging.level.com.ceb.portal=DEBUG

# CORS Configuration (handled in SecurityConfig)
app.cors.allowed-origins=http://localhost:5173,http://localhost:3000,http://localhost:8000

# External API Configuration
external.api.base-url=http://10.128.1.59:6001
external.api.username=CEBkiosk
external.api.password=kiosk#$CEB
